"""The shared retrieval path.

Fusion, ordering, truncation and the pipeline that ties them together. This
package is identical for the evaluation path and the service path; only the
indexes beneath it differ (ADR-0001).
"""
